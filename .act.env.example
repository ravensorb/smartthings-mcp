# .act.env — Environment variables for nektos/act local runs
# Copy to .act.env and fill in values
#
# These map to env.* in the workflow

# No env vars currently required; add custom overrides here if needed.
